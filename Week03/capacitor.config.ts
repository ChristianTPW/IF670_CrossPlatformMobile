import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '32062-ChristianTeguhPrasetyaWidjaja',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
